apiVersion: v1
kind: Pod

metadata:
  name: multi-container-pod

spec:
  containers:
    - name: container-1
      image: nginx

    - name: container-2
      image: centos
      command: ["/bin/bash"] 
      args: ["-c","sleep 200"]
